<!DOCTYPE html>
<html lang="<%= config.language %>">
    <%- partial('_partial/head') %>
    <body class="custom-background">
        <%- partial('_partial/header') %>

        <% if (page.type !== "404") { %>

            <% if (theme.sidebar.location === 'left' && page.sidebar !== false) { %>
                <%- partial('_partial/sidebar') %>
            <% } %>

            <% if (page.sidebar !== false) { %>
                <section class="kr-main-col col-md-<%- (theme.sidebar.location === 'left' || theme.sidebar.location === 'right') ? '8' : '12' %>">
            <% } %>

        <% } %>

            <%- body %>

        <% if (page.type !== "404") { %>
            <% if (page.sidebar !== false) { %>
                </section>
            <% } %>

            <% if (theme.sidebar.location === 'right' && page.sidebar !== false) { %>
                <%- partial('_partial/sidebar') %>
            <% } %>

        <% } %>

        <%- partial('_partial/footer') %>
        <%- partial('_partial/after-footer') %>
    </body>
</html>